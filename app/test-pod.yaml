apiVersion: v1
kind: Pod
metadata:
  name: dynamodb-test
spec:
  serviceAccountName: dynamodb-sa

  containers:
  - name: aws-cli
    image: amazon/aws-cli:latest
    command:
      - sleep
      - "3600"